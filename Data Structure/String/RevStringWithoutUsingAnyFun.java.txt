public class Main
{
	public static void main(String[] args) {
    
//     Reverse a string without using a length function

    String str = "Onkar";
    int count = 0;
     try {
        while(true) 
        {
            str.charAt(count);  // char at index 0 if yes ok                            is there is char at that index
            count ++;           // then count get increment  by 1 and again it checks ^ 
        }
     } catch(Exception e)
       {
            String rev = "";
            for(int i = count - 1 ; i >= 0 ; i--)
            {
                // System.out.println(str.charAt(i));
                
                rev = rev + str.charAt(i);
            }
            
            System.out.println(rev);
       }
        

    }
}
